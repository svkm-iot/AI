% Graph

edge(a,b).
edge(a,c).
edge(b,d).
edge(b,e).
edge(c,g).
edge(e,g).

% Goal state

goal(g).

% DFS

solve(Node, [Node]) :-
    goal(Node).

solve(Node, [Node|Path]) :-
    edge(Node, Next),
    solve(Next, Path).


% Query 

?- solve(a, Solution).
